---
id: input-number
title: input-number
sidebar_label: input-number
slug: input-number
---

### installation
- 直接用 `npm` 或者 `yarn` 安装。
- 统一 `event` 为 `e.target.value` 取值

```bash npm2yarn
npm install @jswork/antd-components
```

### usage
```js
import { AcInputNumber } from '@jswork/antd-components';
```

### preview
```jsx live
<AcInputNumber style={{ width: 200 }} />
```