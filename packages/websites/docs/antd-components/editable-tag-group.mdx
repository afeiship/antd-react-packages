---
id: editable-tag-group
title: editable-tag-group
sidebar_label: editable-tag-group
slug: editable-tag-group
---

### 简介
- 一个常见的 filter 多选逻辑，本质上是另一个 checkbox-group
- 添加 `items/template` 接口
- 统一所有的 `onChagne` 事件
- `空格/tab` 键触发下一个 tag

### 预览
```jsx live
<AcEditableTagGroup
  value={['abc','xyz']}
  onChange={(e) => {
    console.log('evt: ', e.target.value);
  }}
/>
```